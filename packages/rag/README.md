# @openzhiyu/rag-legacy

This package is retained only as a legacy source snapshot. The active RAG
workspace package is `packages/ai/rag`, published inside this monorepo as
`@openzhiyu/rag`.

Do not add new imports from this directory. Move shared RAG work to
`packages/ai/rag` instead.
