---
title: "Getting Started"
description: "Download, connect your platforms, and let OpenZhiyu start working for you — takes about 3 minutes"
icon: "FaRocket"
---

# Getting Started

## System Requirements

| OS          | Minimum Version                                  | Processor                            | RAM  | Disk Space |
| ----------- | ------------------------------------------------ | ------------------------------------ | ---- | ---------- |
| **macOS**   | macOS 12 (Monterey) or later                     | Apple Silicon (M1/M2/M3/M4) or Intel | 4 GB | 500 MB     |
| **Windows** | Windows 10 (64-bit) or later                     | x86_64 / AMD64                       | 4 GB | 500 MB     |
| **Linux**   | Ubuntu 20.04 / Debian 11 or later, or equivalent | x86_64 or ARM64                      | 4 GB | 500 MB     |

> **Network:** Internet connection required for sync and messaging features.
>
> **Linux only:** GTK and libnotify libraries are required (usually pre-installed on desktop Ubuntu/Debian systems).

---

## Download

**macOS**

| Option                                                                                                                                        | Notes                                                      |
| --------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------- |
| [Download .dmg installer (Apple Silicon)](https://github.com/melandlabs/openzhiyu/releases/download/v0.5.0/openzhiyu_0.5.0_macOS_aarch64.dmg) | Recommended for M1/M2/M3/M4 Macs — double-click to install |
| [Download .dmg installer (Intel)](https://github.com/melandlabs/openzhiyu/releases/download/v0.5.0/openzhiyu_0.5.0_macOS_amd64.dmg)           | For Intel Macs                                             |
| Homebrew                                                                                                                                      | For developers — see command below                         |

```bash
# If you have a manual installation, remove it first:
# rm -rf /Applications/openzhiyu.app

# Install via Homebrew
brew tap melandlabs/openzhiyu https://github.com/melandlabs/openzhiyu
brew install --cask openzhiyu

# Upgrade
brew upgrade --cask openzhiyu

# If install or upgrade fails with "App source is not there", clear cache and reinstall:
# rm -rf ~/Library/Caches/Homebrew/openzhiyu
# brew uninstall --cask openzhiyu
# brew install --cask openzhiyu
```

**Linux (Ubuntu/Debian)**

| Option                                                                                                                              | Notes                                               |
| ----------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------- |
| [Download .deb package (x86_64)](https://github.com/melandlabs/openzhiyu/releases/download/v0.5.0/openzhiyu_0.5.0_linux_amd64.deb)  | For x86_64 / AMD64 systems                          |
| [Download .deb package (ARM64)](https://github.com/melandlabs/openzhiyu/releases/download/v0.5.0/openzhiyu_0.5.0_linux_aarch64.deb) | For ARM64 systems (e.g., Raspberry Pi, ARM servers) |

```bash
sudo dpkg -i openzhiyu_0.5.0_linux_amd64.deb
sudo apt-get install -f

# Fix resource path (required for v0.5.0 and earlier)
sudo ln -sf /usr/lib/openzhiyu/_up_ /usr/bin/_up_

# Start the application
openzhiyu
```

> **Note:** The symlink above is needed because of a known packaging issue where the binary looks for resources in `/usr/bin/_up_` instead of `/usr/lib/openzhiyu/_up_`. This will be fixed in a future release.

**Windows**

| Option                                                                                                                        | Notes                                               |
| ----------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------- |
| [Download .exe installer](https://github.com/melandlabs/openzhiyu/releases/download/v0.5.0/openzhiyu_0.5.0_windows_amd64.exe) | Recommended — run the installer to set up OpenZhiyu |
| Winget                                                                                                                        | Coming Soon                                         |

> **Windows SmartScreen:** If Windows shows a warning when running the installer, click **"More info"** then **"Run anyway"**. This is normal — OpenZhiyu is open-source and the code is publicly verifiable. You only need to do this once per machine.
>
> **"Unable to Find Entry Point" / GetSystemTimePreciseAsFileTime error:** If you see an error about `GetSystemTimePreciseAsFileTime` not being found in `kernel32.dll`, your Windows version is too old. OpenZhiyu requires **Windows 10 (build 1607 or later)** or **Windows 11**. Windows XP, Vista, 7, and 8/8.1 are not supported. To check your version, press `Win + R`, type `winver`, and press Enter — or go to **Settings > System > About**.

[View all releases →](https://github.com/melandlabs/openzhiyu/releases)

---

## Permissions

When you first launch OpenZhiyu, the system will ask for a few permissions. Each one has a specific purpose — and **you can decline any of them**. OpenZhiyu will continue to work; you'll just lose the feature that requires that permission.

### macOS

| Permission           | What it lets OpenZhiyu do                                                                                              | Can I decline?                                         |
| -------------------- | ---------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------ |
| **Full Disk Access** | Read your iMessage history from the local database so OpenZhiyu can surface important conversations in your Event feed | Yes — iMessage sync will be skipped                    |
| **Automation**       | Send iMessages on your behalf when you ask OpenZhiyu to reply or notify someone                                        | Yes — you'll receive drafts instead of automatic sends |
| **Notifications**    | Push alerts when important events are detected (urgent emails, mentions, deadlines)                                    | Yes — check the app manually instead                   |

**How to grant or revoke:**

1. Open **System Settings → Privacy & Security**
2. Find the permission category (e.g., **Full Disk Access**, **Automation**, **Notifications**)
3. Toggle OpenZhiyu on or off

You can revisit these settings at any time.

### Windows

| Permission        | What it lets OpenZhiyu do                                                           | Can I decline?                       |
| ----------------- | ----------------------------------------------------------------------------------- | ------------------------------------ |
| **Notifications** | Push alerts when important events are detected (urgent emails, mentions, deadlines) | Yes — check the app manually instead |

Windows SmartScreen may also show a one-time warning when running the installer. Click **"More info"** then **"Run anyway"** — this is normal for open-source software. You only need to do this once per machine.

### Linux

No special permissions are required on Linux. OpenZhiyu uses the standard desktop notification system (libnotify) to send alerts if you have granted notification permissions to other apps, OpenZhiyu will automatically use them too.

---

## AI Configuration

OpenZhiyu uses your existing AI agent configuration to work. To get started, set up your agent's environment variables in `~/.claude/settings.json`.

**Default: Claude**

OpenZhiyu automatically detects your Claude configuration — no extra setup required if you're already using Claude Code or Claude for Desktop.

**Supported Agents**

| Agent    | Status         | Notes                                    |
| -------- | -------------- | ---------------------------------------- |
| Claude   | ✅ Supported   | Auto-detected by default                 |
| Codex    | 🔜 Coming Soon | GitHub Copilot's CLI agent               |
| Pi       | 🔜 Coming Soon | Anthropic's research assistant           |
| OpenClaw | 🔜 Coming Soon | Open-source agent compatible with Claude |
| Hermes   | 🔜 Coming Soon | Browser-use compatible agent             |

> **Note:** OpenZhiyu currently supports Claude. Additional agent support (Codex, Pi, OpenClaw, Hermes) is on the roadmap and will be available in a future release.

**Configuring Your Agent**

Edit `~/.claude/settings.json` to customize your environment:

```json
{
  "skipWebFetchPreflight": true,
  "env": {
    "ANTHROPIC_AUTH_TOKEN": "your-auth-token-here",
    "ANTHROPIC_BASE_URL": "your-base-url-here",
    "ANTHROPIC_MODEL": "your-model-here"
  }
}
```

> **Security:** Your API keys are stored locally in `~/.claude/settings.json`. Always keep your settings file private.
