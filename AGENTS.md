# 项目工程指导思想

本项目后续写方案、写代码、做产品判断时，默认遵循 `docs/architecture/engineering-cybernetics-guiding-principles.md` 中整理的钱学森《工程控制论》式工程原则。

核心口径：

> 先观测，再建模；先闭环，再自动；先边界，再行动；先稳定，再智能。

## 写方案时

方案不能只描述“让智能体做什么”，必须说明：

- 被控对象是什么。
- 观测输入是什么，是否新鲜可信。
- 状态存在哪里，是否结构化、可追溯。
- 控制动作是什么，是否幂等、可审计、可回滚。
- 安全边界是什么，`denied` 是否优先于 `allowed`。
- 反馈信号是什么，下一轮如何使用。
- 异常、扰动、降级和人工接管怎么处理。
- 用户在哪里审核、暂停、纠错。
- 页面如何让用户看懂当前状态。
- 如何测试它不会越权、重复、静默失败。

## 写代码时

优先把功能做成可观测、可恢复的闭环系统：

- 原始事实、理解结果、控制动作、执行结果要分层存储。
- 关键动作必须写事件、带来源、可回放。
- 外部动作、真实资金、发送消息、删除、配置变更默认需要边界和审核。
- 数据源降级要显式暴露 provider/detail/warning，不要伪装成完整数据。
- 自动任务不能只显示“已启动”，要能看到最近运行、下次运行、失败原因和心跳。
- 角色边界要硬化到工具权限和 prompt。一个车间不能越权承担另一个车间的控制目标。
- 对持续任务、交易、记忆、消息处理这类能力，稳定性优先于“看起来聪明”。

## 当前重点约束

- 智能体车间是长期控制器，不是单轮聊天窗口。
- Loop 是车间内的持续任务，不是孤立 cron。
- 记忆系统是状态估计器，不是消息堆积。
- 微信消息先作为不可改写的原始观测事件，再进入 wiki/任务/记忆理解层。
- 自选股猎手负责自选股池维护；操盘交易员只消费当前自选股做模拟盘交易和复盘。
- 量化模拟盘是受控实验环境，目的是暴露策略如何在扰动下失效，而不是证明模型会赚钱。
